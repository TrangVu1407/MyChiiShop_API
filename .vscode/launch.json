{
    "version": "1.0.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Docker: Attach to Node",
            "remoteRoot": "/usr/app",
            "port": 1417,
            "address": "localhost",
            "localRoot": "${workspaceFolder}/server",
            "protocol": "inspector",
            "restart": true
        }
    ]
}
